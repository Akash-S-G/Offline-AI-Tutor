enum ExperimentExecutionMode {
  sensor,
  simulation,
  hybrid,
  observation,
}

enum ExperimentDifficulty {
  easy,
  medium,
  hard,
}

enum ExperimentStatus {
  draft,
  ready,
  running,
  paused,
  completed,
  failed,
}
